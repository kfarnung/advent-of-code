#ifndef DAY18_LIB_H
#define DAY18_LIB_H

#include <string>
#include <vector>

namespace day18
{
    size_t run_iterations(const std::vector<std::string> &input, size_t iteration_count, bool corners_stuck);
}

#endif