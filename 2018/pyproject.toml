[project]
name = "advent-of-code-2018"
version = "0.1.0"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "pytest",
    "pytest-xdist",
    "ruff>=0.16.4",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
